# Somente desenvolvimento: o Vite carrega .env.development apenas com
# `npm run dev`. Nao usar .env.local aqui — aquele e lido tambem no build
# de producao e assaria "localhost" no bundle, quebrando o acesso pela rede.
VITE_API_URL=http://localhost:8002
