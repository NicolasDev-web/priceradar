# Usado no `npm run build` (modo production por padrao do Vite). Aponta para
# o backend exposto via Tailscale Funnel — nao e segredo, e so a URL publica.
VITE_API_URL=https://computador-do-nicolas.tail8fe935.ts.net
