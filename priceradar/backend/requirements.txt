fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Banco (SQLite async) — faltavam aqui, instalação limpa quebrava
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Coleta
httpx>=0.27.0
# curl-cffi imita a assinatura TLS de um Chrome real. É o que faz VivaReal,
# Zap e ImovelWeb responderem 200 em vez de 403 sem gastar cota de proxy.
curl-cffi>=0.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# Playwright é o último nível de fallback (portais que exigem renderizar JS).
# Requer `playwright install chromium` após o pip install.
playwright>=1.44.0

# Refino e exportação
scikit-learn>=1.4.0
numpy>=1.26.0
openpyxl>=3.1.0

# Testes
pytest>=8.0.0
pytest-asyncio>=0.23.0
